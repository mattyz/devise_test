= form_with(model: article, local: true) do |form|
  - if article.errors.any?
    #error_explanation
      %h2
        = pluralize(article.errors.count, "error")
        prohibited this article from being saved:
      %ul
        - article.errors.full_messages.each do |message|
          %li= message
  .form-group
    = form.text_field :title, class: 'form-control', placeholder: 'Title of Article'
  .form-group
    = form.text_area :body, class: 'form-control', placeholder: 'Article Content'
  .form-group
    = form.text_field :image_url, class: 'form-control', placeholder: 'Image Location'
  .c-select
    = form.collection_select :category_id, Category.all, :id, :name, {prompt: 'Select a Category'}, class: 'form-control'
  .form-group
    = form.submit class:'btn btn-default btn-primary'
